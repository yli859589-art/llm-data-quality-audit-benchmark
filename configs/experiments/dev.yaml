{
  "experiment_key": "dev",
  "mode": "dev",
  "dataset_config": "configs/data/wikitext2_smoke.yaml",
  "dataset_configs": ["configs/data/wikitext2_smoke.yaml"],
  "allow_fallback_in_data": true,
  "required_real_data": false,
  "target_keep_rate": 0.6,
  "seeds": [13, 17, 19],
  "baselines": [
    "raw",
    "random_same_keep_rate",
    "length_filter",
    "c4_gopher_heuristic",
    "dedup_only",
    "perplexity_quality_ngram",
    "independent_quality_score"
  ],
  "model_config": "configs/models/tiny.yaml",
  "training_budget_tokens": 10000
}
