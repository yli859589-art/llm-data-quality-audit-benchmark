{
  "mode": "full",
  "dataset": "configs/datasets/tiny_shakespeare.yaml",
  "seeds": [23, 42, 3407],
  "equal_character_budget": true,
  "network_required": false,
  "note": "Larger local multi-seed configuration. Additional public datasets should be run before a submission."
}
