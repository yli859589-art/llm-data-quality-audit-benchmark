{
  "experiment_name": "small_bpe_protocol",
  "dataset_path": "",
  "dataset_manifest_path": "",
  "tokenizer_manifest_path": "configs/tokenizers/bpe16k_protocol.yaml",
  "filter_manifest_path": "",
  "model_config_path": "configs/models_v2/small_20m_bpe_protocol.yaml",
  "output_dir": "artifacts/training_step5/small_bpe_protocol",
  "scope": "main_protocol",
  "seed": 42,
  "max_steps": 0,
  "batch_size": 0,
  "learning_rate": 0.0003,
  "weight_decay": 0.1,
  "warmup_steps": 1000,
  "gradient_clip": 1.0,
  "eval_interval": 100,
  "save_checkpoint": true,
  "device": "cuda_or_cpu",
  "precision": "float32_or_bfloat16",
  "resume_from": "",
  "smoke_only": false,
  "implemented_but_not_run": true,
  "completed": false,
  "notes": "Small BPE training protocol only; not completed in Step 5."
}
